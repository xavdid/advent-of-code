#!/usr/bin/env python3

import argparse
import sys


def main(day, year, slow):
    try:
        solver = __import__(
            f"solutions.{year}.code.day_{day}", fromlist=["Solution"]
        ).Solution
    except ImportError as e:
        print(f"solution not found for day {day} ({year})")
        sys.exit(1)

    s = solver(slow)
    s.print_solutions()


parser = argparse.ArgumentParser(
    description="Run a day of Advent of Code", prog="advent"
)

parser.add_argument("day", type=int)
parser.add_argument("--year", default=2018, type=int)
parser.add_argument("--slow", action="store_true")

args = parser.parse_args()
main(args.day, args.year, args.slow)
